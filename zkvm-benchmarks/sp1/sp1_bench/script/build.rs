fn main() {
    // Building the RISC-V guest program is handled separately via:
    //   cargo +succinct build -p fibonacci-program --target=riscv32im-succinct-zkvm-elf --release
    // This build script intentionally does nothing.
}
